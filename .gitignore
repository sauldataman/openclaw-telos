# User's personal telos data — never commit
telos/
backups/
updates.md

# Eval workspace (skill-creator test artifacts)
telos-workspace/

# OS
.DS_Store

# Build artifacts — not for version control
*.skill
dist/
